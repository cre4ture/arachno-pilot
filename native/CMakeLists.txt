cmake_minimum_required(VERSION 3.22)
project(arachno_native LANGUAGES CXX)

# This directory is intentionally thin.
# Add small bridge targets here when a Jetson-specific C++ API is worth wrapping
# behind a Rust-facing boundary.
